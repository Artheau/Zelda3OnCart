!ROM_NAME = "THE LEGEND OF ZELDA"    ;max 21 characters, will be padded by spaces

!END_OF_ROM = "$3fffff"   ;used to expand the ROM, and to fill

!SCREEN_HEIGHT = "$E0"

!BIG_TILE_HEIGHT = "$10"   ;for code readability, mostly.  I don't recommend changing this
